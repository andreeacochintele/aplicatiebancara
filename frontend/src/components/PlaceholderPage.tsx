export function PlaceholderPage({ title }: { title: string }) {
  return (
    <section>
      <h2>{title}</h2>
      <p>This section is a Phase 1 placeholder. Functionality is added in a later phase.</p>
    </section>
  );
}
